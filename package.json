{
  "name": "zbuilder-admin-panel",
  "version": "1.0.0",
  "description": "ZBuilder Custom Fallback Ad admin dashboard. Uses GitHub repo as the database.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  },
  "license": "UNLICENSED",
  "private": true
}
